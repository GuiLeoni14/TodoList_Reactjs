import styled from 'styled-components';
import * as Label from '@radix-ui/react-label';
export const Container = styled(Label.Root)``;

export const Input = styled.input`
    background-color: #000;
`;
